# Copy this file to .env.local and fill in values.

# Any long random string — used to sign the session cookie (HMAC).
AUTH_SECRET=change-me-to-a-long-random-string

# Comma-separated access codes. "label:code" or bare "code".
# With the value below, the access code is: demo
ACCESS_CODES=demo
